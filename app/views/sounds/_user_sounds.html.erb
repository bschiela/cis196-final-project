<% if @user.sounds.size == 0 %>
	<label>There are no sounds here currently.</label>
<% else %>
	<table style="table-layout:fixed">
		<col style="width:10%"/>
		<col style="width:30%"/>
		<col style="width:60%"/>
		<tbody>
		<% @user.sounds.each do |sound| %>
			<tr>
				<td><%= image_tag sound.image.url, size: "80x80" %></td>
				<td align="center" style="vertical-align:middle">
					<%= link_to "#{sound.name}", user_sound_path(@user.id, sound.id) %><br/>
					<audio preload="none" controls><source src="<%= sound.audio.url %>"/></audio>
				</td>
				<td style="vertical-align:middle; word-wrap:break-word"><%= sound.description %></td>
			</tr>
		<% end %>
		</tbody>
	</table>
<% end %>
